<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <!--<item name="android:textColor">@color/colorBlack</item>-->
        <!--<item name="actionMenuTextColor">@color/colorBlack</item>-->
    </style>

    <!-- Toolbar Theme -->
    <style name="MyToolBarTheme" parent="Theme.MaterialComponents.Light">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <!-- PopUp Theme -->
    <style name="MyPopupTheme" parent="Theme.MaterialComponents.Light">
        <item name="background">@color/colorOptionsMenuBG</item>
    </style>

</resources>